# S3 Service

This module currently creates one test S3 bucket only.

Created bucket:

```text
testing-bucket-${bucket_suffix}
```

Enabled settings:

- Versioning
- Server-side encryption
- Block public access

No folders or prefixes are created in the bucket.
